compiled with DEV C++

ass 2-1: allows user to set dimensions and populate two matricies, prints
those matricies, calls a function to multiply matricies with standard matrix
multiplication rules and prints the final matrix.
